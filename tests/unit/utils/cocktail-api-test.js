import cocktailApi from 'and-ember-hack/utils/cocktail-api';
import { module, test } from 'qunit';

module('Unit | Utility | cocktail-api', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = cocktailApi;
    assert.ok(result);
  });
});
